<form method="post" action="{{ path('app_batiments_delete', {'id': batiment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ batiment.id) }}">
    <button class="btn">Delete</button>
</form>
